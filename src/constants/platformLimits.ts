/** Keep this value in sync with the tracks create guard in firestore.rules. */
export const MAX_STORED_TRACKS_PER_ARTIST = 10

/** Keep this value in sync with platformSettings/default.minimumPayoutMinor. */
export const MINIMUM_ARTIST_PAYOUT_MINOR = 2500
